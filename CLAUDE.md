# AgentEval — מדריך עיצוב לפרויקט (CLAUDE.md)

> קובץ זה נקרא אוטומטית בכל session של Claude Code בריפו הזה. כל עבודה עיצובית — שינוי, תוספת, או פיצ'ר חדש — חייבת לעמוד בהנחיות הבאות. אם יש סתירה בין בקשה חדשה לבין הנחיה כאן, יש לשאול את המשתמש ולא להניח.

## זהות המותג

AgentEval היא פלטפורמת QA ארגונית לסוכני AI. הזהות הוויזואלית: **בהירה, רכה, ומדויקת** — בהשראת צורת/אנרגיית ממשקי גיימינג מודרניים (Discord, Riot Games: עיגול בינוני, טיפוגרפיה בטוחה בעצמה), אבל עם פלטת פסטל עדינה במקום צבעוניות עזה. התחושה הכללית: מינימלי ומדויק, חדשני/טכנולוגי, ומעל הכל — **מרגיש כמו עבודת אדם**, לא כמו טמפלט שנוצר ע"י AI.

**אסור בהחלט:**
- אימוג'ים בשום מקום בממשק. להשתמש באייקוני קו (SVG stroke, בסגנון Lucide/Feather, `stroke="currentColor"`) בלבד.
- אלמנטים משחקיים ליטרליים (בלי XP bars, בלי מדליות, בלי דירוגים/rank badges) — הסגנון המשחקי מתבטא רק בצורה ובאנרגיה, לא בתוכן.
- צבעים בוהקים/ניאון/רוויים מדי — הפלטה כאן פסטלית ורכה במכוון.
- רקעים לבנים שטוחים לגמרי בלי שום גוון/עומק עדין.
- צללים גנריים ("2px 4px רגיל בלי מחשבה") — כל צל שכבתי ומדויק.
- פונטי ברירת מחדל של המערכת בכותרות.
- ריווחים לא עקביים (ערכים אקראיים שלא נגזרים משיטה קבועה).

## פלטת צבעים

```
--bg:            #f8f8fc   /* רקע בסיס — לבן קריר עם נגיעת לבנדר עדינה */
--bg-soft:       #f1f1fa
--panel:         #ffffff   /* כרטיסים/פאנלים */
--panel-2:       #fbfbfe
--line:          #e6e6f2

--brand:         #8a93f0   /* לבנדר-אינדיגו — הצבע הפעיל היחיד (כפתורים, מסגרות נבחרות, לוגו) */
--brand-light:   #c7cfff   /* טון בהיר — רקעים עדינים, hover, chips לא-פעילים */

--accent-peach-bg:   #ffd4c2   /* אפרסק — רקע לתגיות/הדגשות משניות */
--accent-peach-text: #b5622f   /* אפרסק כהה — טקסט על רקע אפרסק, שומר קונטרסט */
--accent-mint-bg:    #c2f5e9   /* מנטה — רקע לתגיות/הדגשות משניות אחרות */
--accent-mint-text:  #1e9e82   /* מנטה כהה — טקסט על רקע מנטה */

--safe:    #1e9e82   /* עבר בנהלים — משתמש בגוון המנטה הכהה, שומר על אחידות הפלטה */
--safe-bg: #e3faf3
--warn:    #b5790a
--warn-bg: #fdf1de
--danger:  #d6363c
--danger-bg: #fdeaeb

--text-primary: #12162a
--text-dim:     #454e66
--text-faint:   #6b7386
```

**חוק שימוש:**
- `--brand` (לבנדר-אינדיגו) הוא **הצבע היחיד לפעולה** — כפתור ראשי, מסגרת נבחרת, לוגו. אין צבע שני שמתחרה איתו על תשומת לב.
- אפרסק ומנטה הם **צבעי הדגשה משניים בלבד** — תגיות, badges, אזורי הבחנה קלים. לא לכפתורים ראשיים ולא לטקסט ראשי.
- אדום/ירוק/כתום סמנטיים (עבר/אזהרה/כשל) נשארים ברורים ומוסכמתיים — אף פעם לא מוחלפים בצבעי המותג, גם אם זה "מתאים לפלטה". בהירות המשמעות (מה עבר, מה נכשל) חשובה יותר מעקביות צבעונית טהורה.
- כשמציבים טקסט על רקע צבעוני (badge/chip), תמיד להשתמש בגוון ה-`-text` הכהה המתאים מאותה משפחה (למשל `--accent-peach-text` על `--accent-peach-bg`) — לא שחור גנרי, לא `--text-primary`.

## טיפוגרפיה

- **כותרות (H1/H2/לוגו):** Archivo, sans-serif, משקל 700-800.
- **גוף טקסט (עברית):** IBM Plex Sans Hebrew, משקלים 400/500/600.
- **נתונים/מונו (ציונים, timestamps, תגיות טכניות):** IBM Plex Mono.
- כותרת ראשית תמיד עם נשימה משמעותית סביבה.
- ניגודיות טקסט מול רקע — לפחות 4.5:1 (AA). `--text-primary` (#12162a) על `--panel`/`--bg` עומד בזה בקלות; לוודא גם עבור כל טקסט חדש על רקעים בהירים מהפלטה.

## עומק וצללים

- שיטה דו-שכבתית עדינה: שכבה חדה וצמודה (`0 1px 2px rgba(18,22,42,.04)`) + שכבה רכה ומפוזרת (`0 10-24px rgba(18,22,42,.06-.08)`).
- כפתור ראשי: צל בגוון ה-`--brand` עצמו, מתעצם קלות ב-hover + הרמה עדינה (`translateY(-2px)`).
- כרטיס נבחר/פעיל: מסגרת `--brand` + `--brand-light` כרקע + צל תואם.
- בלי glassmorphism/blur כברירת מחדל — משטחים לבנים נקיים.

## צורה (Shape Language)

- עיגול בינוני בכל מקום — כפתורים וכרטיסים ברדיוס 10-14px, לא פינות חדות ולא פיל מלא (למעט תגיות/chips קטנות שיכולות להיות פיל).
- זו הנקודה שמביאה את "אנרגיית הגיימינג" — עיגול בטוח, נעים, לא צבאי/קר.

## אנימציות

- כניסת אלמנטים: fade + slide עדין, ב-stagger קצר בין אלמנטים עוקבים.
- Hover: מעבר חלק על צבע/צל/transform יחד (`transition: all .2s cubic-bezier(.4,0,.2,1)`).
- תוצאות/פידבק (למשל תוצאת שיפוט): fade-in משולב עם slide קל.
- לכבד תמיד `prefers-reduced-motion: reduce`.

## גריד ומרווחים

- יחידת spacing קבועה (כפולות 4px: 4/8/12/16/20/24/32/48).
- rhythm עקבי בין כל הסקציות — לא סקציה עם ריווח 20px והבאה עם 35px בלי סיבה.
- ריווח **מאוזן**: נושם, אבל לא מבזבז מקום סתם — לא צפוף כמו dashboard, לא ריק כמו landing page יוקרתי מדי.

## אייקונים

- SVG stroke-style בלבד, עובי קו אחיד לאורך כל המוצר.
- 16-18px inline עם טקסט, 24-32px לאלמנטים בולטים (כרטיסי פרסונה, אייקוני מצב).
- `stroke="currentColor"` — יורש מהטקסט הסובב אלא אם יש סיבה סמנטית ברורה.

## RTL / דו-לשוניות

- כל רכיב חדש נבדק בעברית (RTL) ובאנגלית (LTR).
- תכונות CSS לוגיות (`margin-inline-start`, `border-inline-end` וכו') ולא `left/right` קשיח.

## לפני כל שינוי עיצובי

1. קרא את הקובץ הזה קודם.
2. אם משהו לא ברור/סותר — שאל, אל תנחש.
3. שמור על 100% מהפונקציונליות הקיימת (API calls, לוגיקת state, retry logic) — שינויים עיצוביים הם CSS/HTML בלבד, אף פעם לא לוגיקה.
4. בסוף כל שינוי: git commit + push.
